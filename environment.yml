name: "base"
channels:
  - defaults
dependencies:
  - numpy
  - pandas
  - matplotlib
  - ipywidgets
  - plotly=4.14.3
  - hdfs3
  - pip
  - pip:
      - scikit-learn
      - geopandas
      - voila=0.2.7
      - pyarrow
# - add packages here
# - one per line
prefix: "/opt/conda"

